{{page-title "Contacts"}}
<div class="col-12">
<h1 class="mb-5">Contacts</h1>
<input type="text" class=" col-12 col-md-5 form-control" placeholder="Filtrer">
<table class="table col-12 col-md-5">
  <thead>
  <tr>
    <td>Nom</td>
    <td>Prénom</td>
    <td>Email</td>
    <td>Action</td>
  </tr>
  </thead>
  <tbody>
  {{#each @model.contacts as |contact|}}
    <tr>
      <td>{{contact.nom}}</td>
      <td>{{contact.prenom}}</td>
      <td>{{contact.email}}</td>
     <td>
        <LinkTo class="btn btn-warning" @route="contacts.update"   @model={{ contact.id }} >Modif.</LinkTo>
        <button {{action "delete" contact}}class="btn btn-danger ">Suppr.</button>
      </td>
    </tr>
  {{/each}}
  </tbody>
  </table>
  
        <LinkTo class="btn btn-primary" @route="contacts.add">Ajouter...</LinkTo>

        {{#if @model.deletedsCount}}
        <button class="btn btn-danger" {{action "cancelDeletion" @model.deleteds}}>Annuler la suppression ( {{@model.deletedsCount}} ) .</button>
        {{/if}}

    

</div>
{{outlet}}